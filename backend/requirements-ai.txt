# Optional AI stack. The current core .venv does not have torch installed, so these are
# minimum compatibility constraints rather than fabricated local pins (HYG-01: a version here
# must come from `pip freeze` on a machine that actually has it installed, never guessed).
# To pin these once the AI stack is installed:
#   .venv/bin/pip freeze | grep -E '^(torch|torchvision|torchxrayvision|opencv-python-headless|pyarrow)=='
# then replace the >= bounds below with the exact == versions that command prints.
-r requirements.txt
torch>=2.6
torchvision>=0.21
torchxrayvision>=1.3
opencv-python-headless>=4.10
pyarrow
