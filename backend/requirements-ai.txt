# Optional AI stack. Pinned from `.venv/bin/pip freeze` on this machine (round 3), after
# `pip install -r requirements-ai.txt --extra-index-url https://download.pytorch.org/whl/cpu`.
# Re-pin the same way if you ever bump these: install, then
#   .venv/bin/pip freeze | grep -E '^(torch|torchvision|torchxrayvision|opencv-python-headless|pyarrow)=='
-r requirements.txt
torch==2.14.0
torchvision==0.29.0
torchxrayvision==1.5.4
opencv-python-headless==5.0.0.93
pyarrow==25.0.1
